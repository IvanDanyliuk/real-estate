import { StyleProps } from '../../../../../components/types';

export const styles: StyleProps = {
  form: {
    display: 'flex',
    flexDirection: 'column',
    gap: 2
  },
  triggerBtn: {
    padding: '0 1rem',
  }
}