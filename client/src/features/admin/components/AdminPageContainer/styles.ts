import { StyleProps } from '../../../../components/types';

export const styles: StyleProps = {
  header: {
    marginBottom: 1,
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
};