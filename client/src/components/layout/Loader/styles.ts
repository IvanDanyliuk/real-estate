import { StyleProps } from '../../types';

export const styles: StyleProps = {
  component: {
    wdith: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    bacgroundColor: 'primary.light',
    color: 'primary.main',
  },
};